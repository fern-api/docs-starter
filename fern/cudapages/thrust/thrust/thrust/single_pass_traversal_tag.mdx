---
title: thrust::single_pass_traversal_tag
description: "Tag type for iterators allowing single pass traversal."
---

Tag type for iterators allowing single pass traversal.

```cpp showLineNumbers={false}
#include <thrust/iterator_traversal_tags.h>
```

**Inherits from:** `thrust::incrementable_traversal_tag` (public)
