---
title: thrust::is_error_condition_enum
description: "A metafunction returning whether or not the parameter is an [`error_condition`](/library/api/thrust::error_condition) enum."
---

A metafunction returning whether or not the parameter is an [`error_condition`](/library/api/thrust::error_condition) enum.

```cpp showLineNumbers={false}
#include <thrust/error_code.h>
```

<AccordionGroup>
<Accordion title="Template parameters">

<ParamField path="T" type="typename">
</ParamField>

</Accordion>
</AccordionGroup>

**Inherits from:** `thrust::detail::false_type` (public)
