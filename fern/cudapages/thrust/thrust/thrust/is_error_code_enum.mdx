---
title: thrust::is_error_code_enum
description: "A metafunction returning whether or not the parameter is an [`error_code`](/library/api/thrust::error_code) enum."
---

A metafunction returning whether or not the parameter is an [`error_code`](/library/api/thrust::error_code) enum.

```cpp showLineNumbers={false}
#include <thrust/error_code.h>
```

<AccordionGroup>
<Accordion title="Template parameters">

<ParamField path="T" type="typename">
</ParamField>

</Accordion>
</AccordionGroup>

**Inherits from:** `thrust::detail::false_type` (public)
