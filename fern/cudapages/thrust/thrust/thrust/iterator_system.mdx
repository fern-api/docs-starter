---
title: thrust::iterator_system
description: "Trait obtaining the iterator system of an iterator type, usually as the systems tag type."
---

Trait obtaining the iterator system of an iterator type, usually as the systems tag type.

```cpp showLineNumbers={false}
#include <thrust/iterator_traits.h>
```

<AccordionGroup>
<Accordion title="Template parameters">

<ParamField path="Iterator" type="typename">
</ParamField>

</Accordion>
</AccordionGroup>

**Inherits from:** `thrust::detail::iterator_system_impl< Iterator >` (public)
